# --- DATABASE CONFIG ---
# Used by docker-compose for the 'db' service (Postgres) and the NestJS 'app' service

DB_USERNAME=myuser
DB_PASSWORD=mysecurepassword
DB_DATABASE=fullstackdb

DATABASE_URL=postgresql://myuser:mysecurepassword@db:5432/fullstackdb

# --- APPLICATION & AUTH CONFIG ---

# Required by Better-Auth for session encryption and JWT signing
AUTH_SECRET="A_VERY_LONG_AND_SECURE_RANDOM_STRING_FOR_BETTER_AUTH"

# NestJS Server Port
PORT=3000

# Compose project name to avoid name collisions with other projects
# You can change this to any short identifier (no spaces). Docker Compose will
# use it as the project prefix for container and volume names.
COMPOSE_PROJECT_NAME=multi_tenant_backend

# Add other Better-Auth specific keys here if required by the service
# BETTER_AUTH_API_KEY=your_key_here
